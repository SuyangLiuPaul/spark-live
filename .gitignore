# Local pre-fill of API keys — NEVER commit, publicly readable if deployed
public/config.js

node_modules/
.netlify/
.DS_Store

# Wrangler's local build/state cache for the relay Worker
relay/.wrangler/
